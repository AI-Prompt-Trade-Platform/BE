spring.application.name=Prumpt_BE

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

# DataSource
spring.datasource.url=jdbc:mysql://127.0.0.1:3306/prumpt?allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=cell4086
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Spring DevTools LiveReload
spring.devtools.livereload.enabled=true

# Spring Security User
spring.security.user.name=root
spring.security.user.password=1234

# Thymeleaf cache disable
spring.thymeleaf.cache=false

# Logging level to debug
logging.level.org.springframework=DEBUG

# Auth0 ?? ??? URL (Issuer)
auth0.issuerUri = https://dev-q64r0n0blzhir6y0.us.auth0.com/

# Auth0 API ???(=Audience)
auth0.audience  = https://api.prumpt.local

# Spring Security Resource Server (?? ??)
spring.security.oauth2.resourceserver.jwt.issuer-uri=https://dev-q64r0n0blzhir6y0.us.auth0.com/
#spring.security.oauth2.resourceserver.jwt.jwk-set-uri=https://dev-q64r0n0blzhir6y0.us.auth0.com/.well-known/jwks.json

# OpenAI API Key
spring.ai.openai.api-key=${OPENAI_API_KEY}

# AWS S3
#aws.region=ap-northeast-2
#aws.s3.bucket=prumpt
#aws.s3.access-key=${AWS_ACCESS_KEY_ID}
#aws.s3.secret-key=${AWS_SECRET_ACCESS_KEY}